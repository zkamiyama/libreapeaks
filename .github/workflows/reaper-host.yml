name: reaper-reference-host-acceptance
on:
  push:
    branches: [feature/reaper-rpkx-preserving-plugin]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: reaper-host-${{ github.ref }}
  cancel-in-progress: true
jobs:
  host:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-15, windows-2025]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Linux runtime
        if: runner.os == 'Linux'
        run: sudo apt-get update -qq && sudo apt-get install -y xvfb libgtk-3-0 libasound2t64 ffmpeg
      - name: Build Rust and real reference extension
        shell: bash
        run: |
          set -euo pipefail
          git clone https://github.com/justinfrankel/reaper-sdk.git .host-sdk
          git -C .host-sdk checkout 490ded57668727fba21482fabc50ba9853a457bb
          cargo test --release --manifest-path examples/reaper_rpkx_extension/Cargo.toml
          cargo build --release --manifest-path examples/reaper_rpkx_extension/Cargo.toml
          python examples/reaper_rpkx_extension/host_tests/setup_host.py --build-only
      - name: Install REAPER 7.79 in isolated workspace
        run: python examples/reaper_rpkx_extension/host_tests/setup_host.py --install-only
      - name: Real host actions and negative controls
        shell: bash
        run: |
          set -euo pipefail
          if [ "$RUNNER_OS" = Linux ]; then
            xvfb-run -a -s '-screen 0 1280x800x24' python examples/reaper_rpkx_extension/host_tests/host_acceptance.py
          else
            python examples/reaper_rpkx_extension/host_tests/host_acceptance.py
          fi
      - name: Extended ordinary edit and media-creation workflows
        shell: bash
        run: |
          set -euo pipefail
          if [ "$RUNNER_OS" = Linux ]; then
            xvfb-run -a -s '-screen 0 1280x800x24' python examples/reaper_rpkx_extension/host_tests/host_extended.py
          else
            python examples/reaper_rpkx_extension/host_tests/host_extended.py
          fi
      - name: Verified native and RPKX-size benchmarks
        shell: bash
        run: |
          set -euo pipefail
          if [ "$RUNNER_OS" = Linux ]; then
            xvfb-run -a -s '-screen 0 1280x800x24' python examples/reaper_rpkx_extension/host_tests/benchmark.py
          else
            python examples/reaper_rpkx_extension/host_tests/benchmark.py
          fi
      - name: Verify same-build completion manifest
        if: always()
        run: python examples/reaper_rpkx_extension/host_tests/completion.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: reaper-host-${{ matrix.os }}
          path: |
            host-results/
            host-build/reaper_rpkx.*
            host-build/Release/reaper_rpkx.*
            host-diagnostic/reaper_rpkx.*
            host-diagnostic/Release/reaper_rpkx.*
          if-no-files-found: error
          retention-days: 7
