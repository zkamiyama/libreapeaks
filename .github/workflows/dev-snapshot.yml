name: dev-snapshot

on:
  push:
    branches: [hardening-reapeaks-cache-ffmpeg]
    paths:
      - '.github/workflows/dev-snapshot.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
      - name: Package full source tree
        shell: bash
        run: |
          set -euo pipefail
          git rev-parse HEAD > snapshot-commit.txt
          tar \
            --exclude='./.git' \
            --exclude='./repo-snapshot.tar.gz' \
            -czf repo-snapshot.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: repo-snapshot-${{ github.sha }}
          path: |
            repo-snapshot.tar.gz
            snapshot-commit.txt
          if-no-files-found: error
          retention-days: 3
