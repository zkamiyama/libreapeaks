diff --git a/src/strict_wdl.cpp b/src/strict_wdl.cpp
index 7e1f11e..ce60aef 100644
--- a/src/strict_wdl.cpp
+++ b/src/strict_wdl.cpp
@@ -45,7 +45,7 @@ bool valid_interleaved_span(long long frames, int channels) noexcept {
         std::numeric_limits<std::size_t>::max() / sizeof(double));
 
     return frame_count <= max_pointer_elements / channel_count &&
-           frame_count <= max_double_elements / channel_count;
+           frame_count < max_double_elements / channel_count;
 }
 
 } // namespace
