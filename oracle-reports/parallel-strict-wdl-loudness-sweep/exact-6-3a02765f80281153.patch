diff --git a/src/pyramid.rs b/src/pyramid.rs
index 90e785c..1eccef5 100644
--- a/src/pyramid.rs
+++ b/src/pyramid.rs
@@ -144,7 +144,7 @@ impl WavePyramid {
                         LevelSource::DerivedFromFine { factor },
                     ));
                 }
-                if count <= 1 || div >= source_frames.max(1) {
+                if count <= 1 || div > source_frames.max(1) {
                     break;
                 }
                 let Some(next) = factor.checked_mul(ratio) else {
